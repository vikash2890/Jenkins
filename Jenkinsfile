pipeline {
       // agent any
	agent { docker { image 'maven:3.6.3' } }
        stages {
         stage(build) {
                steps{
                 echo "build"
                     }
		}
         stage(test) {
                steps{
                 echo "build"
                     }
		}
	}
}
